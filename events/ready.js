/**
 * @param {import('discord.js').Client} bot 
*/
module.exports = async (bot) => {
    console.log(bot.user.username + " ready!\nSkelet by Strelok"); //Ты, конечно, можешь поменять на своё, но нужно ли?
};